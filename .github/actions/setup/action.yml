name: Setup
description: Setup Node and install dependencies (repo must already be checked out)

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v7
      with:
        node-version-file: ".nvmrc"
        cache: npm
    - run: npm ci
      shell: bash
